{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && next build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["sin1"],
  "functions": {
    "src/app/api/**/*": {
      "maxDuration": 30
    }
  }
}
